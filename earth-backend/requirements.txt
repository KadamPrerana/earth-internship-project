# ============================================================
# requirements.txt — Python Dependencies for Django Backend
# ============================================================
# This file lists all Python packages required to run the
# Django backend. Docker uses this file to install dependencies
# inside the container with: pip install -r requirements.txt
#
# Why pin versions?
#   Pinning ensures the same versions are installed everywhere
#   (local, Docker, production), preventing "works on my machine" bugs.
# ============================================================

Django==5.2.11                  # Web framework
djangorestframework==3.16.1     # REST API framework for Django
django-cors-headers==4.9.0      # CORS headers for cross-origin requests
mongoengine==0.29.1             # MongoDB ORM/ODM (Object Document Mapper)
pymongo==4.15.4                 # MongoDB driver for Python
Werkzeug==3.1.3                 # Password hashing utilities
PyJWT==2.3.0                    # JWT token generation & validation
